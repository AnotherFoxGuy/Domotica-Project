<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:microcharts="clr-namespace:Microcharts.Forms;assembly=Microcharts.Forms"
             mc:Ignorable="d"
             x:Class="Project_Green.MainPage">

    <StackLayout>
        <Button Clicked="Button_Clicked" Text="Test"/>
        <Label x:Name="IP_Address" Text="here should be an ip address"/>
        <ListView x:Name="GreenhouseList">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ViewCell>
                        <StackLayout Orientation="Horizontal">
                            <Image Source="{Binding Greenhouse_Image}" WidthRequest="64"/>
                            <StackLayout>
                                <Label Text="{Binding Greenhouse_Name}"/>
                            </StackLayout>
                        </StackLayout>
                    </ViewCell>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
        <microcharts:ChartView x:Name="Testchart" HeightRequest="150"/>
    </StackLayout>

</ContentPage>
